name: Publish to npm + Docker Hub

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  npm-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22', registry-url: 'https://registry.npmjs.org' }
      - run: npm publish --access public
        env: { NODE_AUTH_TOKEN: '${{ secrets.NPM_TOKEN }}' }

  docker-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build & Push Docker
        run: |
          docker build -t claudelaw/taichu:latest -t claudelaw/taichu:${{ github.ref_name }} .
          echo "${{ secrets.DOCKER_TOKEN }}" | docker login -u claudelaw --password-stdin
          docker push claudelaw/taichu:latest
          docker push claudelaw/taichu:${{ github.ref_name }}

  docker-aliyun:
    runs-on: ubuntu-latest
    needs: docker-publish
    steps:
      - name: Check Aliyun credentials
        id: aliyun-check
        run: |
          if [ -n "${{ secrets.ALIYUN_ACR_USER }}" ] && [ -n "${{ secrets.ALIYUN_ACR_TOKEN }}" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi
      - name: Mirror to Alibaba Cloud ACR (China)
        if: steps.aliyun-check.outputs.available == 'true'
        run: |
          docker pull claudelaw/taichu:latest
          docker tag claudelaw/taichu:latest registry.cn-hangzhou.aliyuncs.com/claudelaw/taichu:latest
          echo "${{ secrets.ALIYUN_ACR_TOKEN }}" | docker login --username=${{ secrets.ALIYUN_ACR_USER }} --password-stdin registry.cn-hangzhou.aliyuncs.com
          docker push registry.cn-hangzhou.aliyuncs.com/claudelaw/taichu:latest
